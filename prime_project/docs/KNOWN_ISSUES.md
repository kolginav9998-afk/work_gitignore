# PRIME 2.2.0 — известные ограничения текущего релиза

Честный список для 2.2.0 (FINAL mega-task). Полный построчный разбор — `docs/
REQUIREMENTS_MATRIX_FINAL.md`. Разделы про 2.1.2/2.1.1/2.1.0/2.0.1 сохранены ниже без изменений
для истории.

## Не сделано в 2.2.0 (сознательно отложено или не проверено — честно, а не скрыто)

1. **Полный физический реордер колонок "Заказы" по точному списку мастер-задания не
   сделан.** Единственное буквально нормативное требование к порядку ("От кого / площадка" и
   "Продавец" соседствуют) уже выполнено текущим порядком колонок. Полная перестановка всех 24
   полей — высокий риск (это самый сложный, наиболее бизнес-критичный лист в системе) ради
   косметического/организационного требования; вместо этого приоритет отдан реальным
   функциональным дырам (control_validation, которой не было вообще). Если реордер всё же
   нужен для точного соответствия документу — отдельный, узкоскоуп будущий проход,
   ОБЯЗАТЕЛЬНО с полным regression-прогоном `tests/functional_2_1_2.py` после.
2. **Экспорт актов в PDF не реализован** — только ODT (`acts.formats=[ODT, PDF по отдельной
   команде]`). ODT-акт уже полностью автономен (авто-номер, без внешнего номера документа);
   отдельная команда "Экспорт в PDF" не добавлена в этом проходе.
3. **"Инвентаризация" не переведена на точную схему из мастер-задания**
   (`[EI_CODE,Наименование,Место,Учётный остаток,Факт,Разница]`) — сохранила свою прежнюю
   схему (Сессия/Код/Контур/Учёт/Факт/Категория/Подкатегория/Ед.изм.). Колонка "Контур" здесь
   больше не партиционирует остаток (см. фикс `PRIME_StockByLocation` ниже), но осталась как
   информационное поле, а не EI_CODE-центричная схема. Список
   `tests/mandatory_regressions` для Инвентаризации ("exact-EI inventory") при этом выполняется
   на уровне engine — расхождение чисто в наборе колонок листа.
4. **"Журнал" не сведён к точному списку колонок мастер-задания**
   (`[DOC_ID,Тип операции,Дата/время,Источник,Куда,Количество строк,Статус,Комментарий]`) —
   сохранены дополнительные `OP_ID` и "Поставщик/Получатель" (полезная, не форбидден
   информация), "Контур" заменён на "Куда" per spec.
5. **Тултип ввода на "Заказы" (валидация ячейки "Внутренний код") всё ещё упоминает
   Firebird.** Найден при аудите wording ("Введите внутренний код. Если код уже есть в
   Firebird, полное наименование в колонке B заполнится автоматически.") — это ODF
   content-validation input-help, а не обычная ячейка; правка через UNO `TableValidation` API
   не сделана в этом проходе (риск случайно сломать саму валидацию/выпадающий список ради
   косметической правки текста подсказки, который виден только при клике в конкретную ячейку,
   не постоянно). Все остальные постоянно видимые упоминания Firebird/"база" на всех 16
   авторитетных видимых листах устранены и закреплены автоматической проверкой (см.
   `tests/static_checks.py`).
6. **Acts (создание .odt) не подтверждено личным сквозным прогоном в этой сессии** — песочница
   этой разработческой сессии содержит только `libreoffice-calc`, без `libreoffice-writer`
   (тот же пакет, что ставит `.github/workflows/*.yml`, ДО этого прохода — CI-конфиг обновлён,
   теперь ставит и `libreoffice-writer`, так что дальнейшие CI-прогоны реально протестируют это
   сквозным образом). В этой сессии проверено: (а) функция вызывается и принимает только
   внутренний DOC_ID, без внешнего номера документа; (б) при недоступности Writer тест-прогон
   даёт специфическую, узнаваемую ошибку `com.sun.star.lang.IllegalArgumentException` /
   "type detection failed" (не что-то из бизнес-логики PRIME) и корректно помечается как SKIP, а
   не FAIL, в `tests/functional_2_1_2.py`.

## Найдено и исправлено в 2.2.0 (архитектурный дефект, а не только 8 подтверждённых багов)

Помимо 8 явно перечисленных в мастер-задании подтверждённых дефектов (см. `CHANGELOG.md`
[2.2.0]), обнаружен и исправлен ещё один экземпляр ТОЙ ЖЕ проблемы, что и confirmed bug #3
("Наличие" фильтр по контуру вместо происхождения) — `PRIME_03_Catalog.PRIME_StockByLocation`
(используется "Инвентаризацией" и автоподстановкой "Откуда" на "Выдачи") группировал остаток по
(место, контур), а не только по месту: одна и та же физическая позиция после перемещения с
пустым контуром (см. `transfers.rule` фикс) задваивалась на ДВЕ строки вместо суммирования в
одну. Найдено интеграционным тестом (`tests/functional_2_1_2.py`, сценарий "Инвентаризация после
перемещения"), не статическим анализом — практическое подтверждение того, почему headless
UNO-прогон, а не только модельные Python-тесты, остаётся обязательным для этого класса правок.

## Найдено и исправлено в 2.1.2 (три дефекта самого движка StarBasic/UNO — не бизнес-логики)

При реализации живого обновления дочерних receipt-строк "Заказы" (см. `CHANGELOG.md` [2.1.2])
были обнаружены и устранены три ранее незамеченных дефекта headless-сборки LibreOffice, не
связанных с бизнес-логикой PRIME:

1. **"Buffered-array-readback corruption"** — накопление строк в растущий `Variant()`-буфер
   (`ReDim Preserve`) для одной пакетной записи тихо портит данные (пустые/нулевые поля БЕЗ
   единой ошибки), если этому предшествовала запись на другой лист в той же цепочке внешних
   вызовов. Затронуло 6 функций (Issue/Return/Transfer/Adjustment posting, "Возвраты"/
   "Инвентаризация" списки кандидатов, "Наличие"/"Поиск"/"Журнал" обновление) — все переписаны
   на прямую поячейковую запись без буфера. Обнаружено и подтверждено ТОЛЬКО прямым чтением
   содержимого ячеек после "успешного, без единой ошибки" вызова — обычный лог/MsgBox об этом
   не сообщал вообще. См. `REQUIREMENTS_MATRIX_2.1.2.md` H05 для полного списка и метода
   диагностики (десяток одноразовых диагностических Basic-модулей, динамически внедрённых в
   ходе headless-сессии, впоследствии удалённых из продакшен-кода).
2. **`InStrRev` не определена в этой сборке StarBasic** — ломала каждый возврат и экспорт
   актов/отчётов молча (общее сообщение об ошибке StarBasic "Sub-procedure or function
   procedure not defined" не указывает, какая конкретно встроенная функция отсутствует, пока
   не сопоставить `Erl` с исходником). Заменено на собственный `PRIME_LastInStr`.
3. **Committed-only фильтрация отсутствовала в "Поиск"/"Возвраты"** (хотя присутствовала в
   расчёте остатка) — существовало как минимум с 2.1.0, не было замечено раньше, потому что
   для этого нужен PREPARED-затем-FAILED документ, который сложно спровоцировать вручную без
   fault injection. См. уточнение к E14 из 2.1.1 в конце `REQUIREMENTS_MATRIX_2.1.2.md`.

**Практический вывод для будущей разработки в этой headless-сборке**: любая функция, которая
накапливает строки для одной пакетной batch-записи (а не пишет их сразу поячейково), должна
считаться подозрительной, если она может быть вызвана после хотя бы одной другой записи на
другой лист в том же вызове/цепочке кнопок. Модельные Python-тесты (`tests/model_tests.py`) НЕ
могут обнаружить этот класс дефектов — они проверяют алгоритмы, а не поведение конкретного
движка StarBasic/UNO; нужен headless UNO-прогон на реально собранном `.ods` (см.
`tests/functional_2_1_2.py`).

## Осознанно принятое ограничение 2.1.2: нет активной формы расхода для Производства/Деталей

Авторитетный список видимых листов включает "Приход — Производство"/"Приход — Детали" как
активные receipt-формы, но НЕ включает ни один "Расход — ..." лист кроме общего "Выдачи"
(который работает только с общим складом/GENERAL). Это означает: списывать остаток, накопленный
через эти два новых прихода, средствами UI в этом релизе НЕЛЬЗЯ (кроме как раскрыв вручную
архивные "Расход — Производство/Детали", которые остаются read-only архивом истории, не
активной формой). Это явное, буквальное следствие явно данного авторитетного списка листов, а
не упущение — при необходимости добавление активных форм расхода для этих контуров имеет смысл
как отдельный будущий проход.

## Изменение бизнес-модели в 2.1.1 (не баг, осознанное решение владельца продукта)

`PRODUCT_CODE` (`ЕИ-00000001`) больше не идентифицирует общий товар — он идентифицирует ОДНУ
конкретную приходную складскую позицию. Два прихода одного и того же товара у одного
поставщика теперь ВСЕГДА получают разные коды и раздельные остатки, даже если наименование,
артикул и поставщик совпадают буквально. Это не миграция существующих данных "задним числом" —
позиции, созданные до 2.1.1, сохраняют свои существующие коды как есть; правило "новый код на
каждый приход" действует только для приходов, проведённых НАЧИНАЯ с 2.1.1. Полное описание —
`docs/REQUIREMENTS_MATRIX_2.1.1.md` (E01/E02), `docs/CHANGELOG.md`.

## Найдено и исправлено в 2.1.1 (три реальных дефекта, обнаруженных при аудите под новую модель)

1. **Возврат зачислялся в `DEFAULT_LOCATION`/`GENERAL` вместо исходного места/контура
   выдачи.** `PRIME_08_ReturnsInventory.PRIME_Returns_ConductButton` жёстко задавала
   `docLine.LocationTo = PRIME_GetProductField(code, "DEFAULT_LOCATION")` и
   `docLine.Contour = SC_GENERAL` для ВСЕЙ строки возврата, независимо от того, откуда товар
   был реально выдан. Если выдача шла, например, с "Цех-А"/контур "Детали цеха", а
   `DEFAULT_LOCATION` товара — другое место, возврат зачислялся не туда. Исправлено:
   `PRIME_04_Posting.PRIME_PostReturnLines` теперь читает `PRIME_GetLotField(lots(j),
   "LOCATION")`/`"STOCK_CONTOUR")` для КАЖДОЙ конкретной allocation отдельно.
2. **Инвентаризация могла создать движение с пустым `LOT_ID`.** Если найденных по (товар,
   место, контур) партий не хватало на всю расчётную недостачу (устаревший снимок —
   движения появились уже после снятия остатка), `PRIME_PostAdjustmentLines` молча писала
   "безлотовое" списание остатка с пометкой в диагностике. Исправлено: `PRIME_ValidateAdjustment`
   теперь ОТКЛОНЯЕТ весь batch корректировки ДО какой-либо записи, если недостача превышает
   фактически доступный остаток позиции — пользователь должен обновить остатки и повторить.
3. **"Провести все" могло повторно провести уже проведённую строку.** На листах "Выдачи" и
   всех 4 workflow-листах ("Приход/Расход — Цех/Офис"), а также "Перемещения", успешно
   проведённая строка НИКОГДА не помечалась как завершённая (`_PRIME_IssueState`/
   `_PRIME_WFState`/`_PRIME_TransferState` оставались с исходным стабильным ID навсегда, при
   этом видимые бизнес-колонки строки тоже не очищались). Следующий клик "Провести все" после
   добавления новых строк снова подхватывал старую строку в НОВЫЙ batch SOURCE_KEY —
   комбинация "старый ключ + новый ключ" ни разу раньше не встречалась, поэтому проверка
   идемпотентности по SOURCE_KEY не срабатывала, и строка проводилась ПОВТОРНО (двойное
   списание/приход). "Заказы"/"Возвраты"/"Инвентаризация" не были подвержены этому багу —
   у них уже был свой (иной, но тоже корректный) механизм пометки "уже обработано".
   Исправлено единообразно во всех трёх местах: успешно проведённая строка помечается
   префиксом `DONE:` в своей скрытой служебной колонке состояния и полностью исключается из
   дальнейшего сканирования, в том числе при одиночном проведении одной строки.

## Найдено и исправлено в 2.1.1 (согласованность батч-проведения)

Заполненная, но невалидная строка (например, код указан, а количество пустое/некорректное)
раньше молча исключалась из batch на всех 5 batch-conduct листах (Заказы/Выдачи/4 workflow-
листа/Возвраты/Перемещения), а ОСТАЛЬНЫЕ строки батча всё равно проводились — это оставляло
у пользователя ложное впечатление, что обработаны ВСЕ заполненные строки, тогда как одна из
них была молча пропущена. Исправлено: любая заполненная, но невалидная строка теперь
останавливает построение всего batch целиком — ничего не проводится, пока пользователь не
исправит или не очистит эту строку.

## Исправлено в 2.1.0 (было в этом списке для 2.0.1, здесь не повторяется)

Полный список — `CHANGELOG.md`. Самое существенное: COMMITTED OP_ID cache теперь обновляется
сразу после проведения (не только при полной перестройке); FIFO/доступность строго по (товар,
место, контур), а не по товару в целом; TRANSFER/ADJUSTMENT теперь lot-consistent (сохраняют
партийность/lineage); Returns/Documents/Lines фильтруются только по COMMITTED; повторные
частичные возвраты учитывают уже возвращённое по каждой конкретной allocation отдельно;
"Провести все" создаёт один документ вместо N; строки одного товара резервируют остаток друг у
друга; validation больше не создаёт товар до полной проверки плана. Также найден и исправлен не
связанный с перечисленным критический баг: пустая `SYS_PRIME_SEQ` ломала первое же действие в
свежей книге (см. CHANGELOG, `PRIME_ReadTable`).

**Пост-review блокирующее исправление (тот же PR, до merge):** физическая запись нового товара
в `DB_PRIME_PRODUCTS` нарушала transaction protocol — вызывалась ДО записи
`SYS_PRIME_TX.STATE=PREPARED`, а не после. Исправлено: ЕИ-код нового товара теперь только
"подглядывается" (read-only, `PRIME_PeekSequenceValue`) во время валидации, а физическая запись
строки + персистентный сдвиг счётчика (`PRIME_WriteNewProductsIfAny`) происходит строго ПОСЛЕ
PREPARED; до COMMITTED такая строка невидима обычному поиску товара (новая колонка `OP_ID` на
`DB_PRIME_PRODUCTS`, committed-only фильтр в `PRIME_BuildProductIndex`). См. R11/R31 в
REQUIREMENTS_MATRIX и 4 новых fault-injection теста в `tests/model_tests.py`.

## Добавлено сверх мастер-задания (тот же PR)

Контуры остатка (склад/детали цеха/офис), inline-остаток на всех рабочих листах, лист "Наличие"
(переименован из "Остаток"), новые листы "Перемещения"/"Журнал", первая кнопка на "Комплекты",
полный immutable snapshot заказа (29 полей), "Последний приход"/"Дата последнего прихода", 0
dead-кнопок (24 обсолетные кнопки физически удалены из формы и DrawPage — не просто скрыты,
см. R26 в REQUIREMENTS_MATRIX). Подробности — `CHANGELOG.md`.

## Не сделано в 2.1.0 (сознательно отложено или заблокировано средой)

1. **Полная визуальная переработка панели "Инфо"/"Заказы" под точный каталог кнопок из
   мастер-задания.** Вместо неё — точечное скрытие нереализованных кнопок (см. R26 в
   REQUIREMENTS_MATRIX). Замена целого набора кнопок на новый список требует визуальной
   проверки раскладки, недоступной в headless-среде без риска сломать компоновку панели
   вслепую.
2. **Диагностика (`PRIME_13_Diagnostics`) не расширена под контуры/lot lineage 2.1.0** - новые
   проверки (например, "движение с STOCK_CONTOUR, не совпадающим с STOCK_CONTOUR его партии")
   не добавлены в этом проходе.
3. **Полный сквозной прогон `PRIME_PostDocument` через прямой внешний вызов в headless
   LibreOffice воспроизводимо нестабилен** в этой конкретной песочнице - см. следующий раздел.
   Это НЕ то же самое, что "бизнес-логика не работает": все алгоритмы независимо проверены
   чистой Python-моделью (`tests/model_tests.py`, 32/32), а структурная корректность собранного
   `.ods` - `tests/static_checks.py` (93/93) и `tools/static_check_source.py` (216/216).

## Прямой вызов PRIME_PostDocument в headless LibreOffice: детальная находка этой сессии

В процессе создания `tools/run_e2e_diagnostic.py` (прямой вызов Basic-функций в реальном
LibreOffice - тот же метод, которым в предыдущих сессиях был найден баг
`PRIME_ParseFlexibleDate`) обнаружено:

- Метод РЕАЛЬНО работает и нашёл реальный баг: `PRIME_SequenceNext` падал на полностью пустой
  `SYS_PRIME_SEQ` (первое действие в свежей книге) - см. CHANGELOG. После фикса `PRIME_ReadTable`
  генерация DOC_ID/OP_ID подтверждена работающей.
- После этого фикса ПОЛНЫЙ вызов `PRIME_PostDocument` (даже для простейшего прихода уже
  существующего товара, без единой новой строки конфигурации 2.1.0) воспроизводимо возвращает
  "" с внутренней ошибкой "Object variable not set", пойманной собственным обработчиком
  `PostFailed` функции.
- Обширная бисекция (более 10 изолированных тестовых сборок, включая: вызов из того же модуля,
  что и `PRIME_PostDocument`, вместо стороннего диагностического модуля - исключает
  несовпадение ABI типа `PrimeDocPlan` между модулями; уменьшение размера массива `Lines()` с
  999 до 99 - исключает связь именно с поднятием лимита строк 2.1.0; ручную построчную замену
  того же кода локальными переменными без прохождения через `PRIME_ValidateAndExpandPlan` -
  не смогла чисто воспроизвести точку отказа) НЕ смогла свести проблему к одной конкретной
  строке кода.
- Рабочая гипотеза (не подтверждена и не опровергнута до конца): это свойство конкретно
  ВНЕШНЕГО headless `invoke()` через scripting provider в этой песочнице, отличное от того, как
  реальный клик пользователя мышью диспетчеризуется внутри самого LibreOffice - то есть
  расширение (в более сильной, воспроизводимой форме) уже задокументированного ограничения ниже
  (п. 11 "Ограничения тестового покрытия"), а не новый подтверждённый дефект бизнес-логики.
- Честный итог зафиксирован в шапке `tools/e2e_diagnostic_module.bas` и в
  `docs/REQUIREMENTS_MATRIX.md` (R29/R30 = `BLOCKED_BY_EXTERNAL_ENVIRONMENT`, не
  `IMPLEMENTED_VERIFIED` и не "confirmed defect").

## Новая находка этой сессии (пост-review исправление): `base` как имя переменной ломает сборку

При реализации transaction-protocol исправления (R31) обнаружен ещё один недокументированный
StarBasic-дефект, того же класса, что и другие особенности этого проекта (например,
нескоротко-замкнутые `And`/`Or`): использование identifier `base` (`Dim base As Long`) как
ОБЫЧНОГО имени переменной в `PRIME_04_Posting.bas` детерминированно (не флаки) ломало
последующие ОТДЕЛЬНЫЕ структурные top-level `invoke()`-вызовы в СОВЕРШЕННО других модулях в
той же сборке — конкретно, `PRIME_Install_EnsureAllBusinessSheetsOnly` (создание листов
"Перемещения"/"Журнал"/"Комплекты") и переименование "Остаток"→"Наличие" молча переставали
создавать/переименовывать листы, хотя сама функция с `base` физически никогда не вызывалась
(она даже не была привязана ни к одной кнопке) — то есть достаточно самого ПРИСУТСТВИЯ кода,
компилируемого в составе библиотеки "Standard". Явные признаки: обычные простые accessor-вызовы
из ТОГО ЖЕ модуля (`PRIME_LastPostError`) при этом продолжали успешно вызываться через
`invoke()` без исключения — то есть это НЕ классическая ошибка компиляции с диагностируемым
сообщением, а более тонкое воздействие на состояние рантайма библиотеки Basic. Найдено методичной
бисекцией (пошаговый откат/восстановление кода внутри одной функции, ~15 промежуточных сборок).
Исправлено переименованием переменной в `seqBaseVal` — после этого сборка снова стабильно даёт
"rebound 107 of 107"/"removed 24 of 24"/все листы создаются. **Практический вывод для будущей
разработки этого проекта**: избегать identifier `base` как имени переменной в StarBasic-модулях
этого проекта (использовать `baseVal`/`seqBase` и т.п.); при новой необъяснимой "структурная
операция молча ничего не сделала" сначала проверить весь недавно изменённый код на использование
этого и подобных потенциально зарезервированных слов, а не сразу списывать на уже известную
общую нестабильность headless invoke().

---

# PRIME 2.0.1 — известные ограничения релиза 2.0.1 (историческое, оставлено без изменений)

Честный список того, что не работает, не реализовано, или не проверено, на момент этого
релиза. Формулировка `false_claims_of_full_verification_forbidden` из мастер-задания
запрещает утверждать, что что-либо из перечисленного здесь "проверено в реальном
исполнении", если это не так — см. также `TEST_MATRIX.md`.

## Исправлено в 2.0.1 (было в этом списке для 2.0.0, здесь не повторяется)

Полный список — в `CHANGELOG.md`. Кратко, самое существенное: операционный лок теперь
реально блокирует повторный вход (двойной клик); event guard больше не "залипает" после
вложенного события; остаток/FIFO/доступность больше не учитывают PREPARED/FAILED-движения;
откат SOURCE_KEY-кэша при неудачном `store()`; и — самое серьёзное — почти все бизнес-листы
(Выдачи, Приход/Расход-Цех/Офис, Возвраты, Инвентаризация, Остаток, Остаток-Заказы,
База-Поиск) были в 2.0.0 фактически НЕ мигрированы с раскладки 1.4.1 (декоративная "шапка"
шаблона + легаси `_WMS_*`-колонки) и не совпадали со схемой, которую ожидал остальной код —
исправлено централизованным `PRIME_FORM_SCHEMA`-реестром (`header_schema_registry`) и общей
функцией миграции колонок. Легаси WMS_*-модули (38 шт.) удалены из собранного `.ods`.

## Добавлено сверх мастер-задания 2.0.1 (по отдельной рекомендации, тот же PR)

- **Гибкий разбор даты и автоматический статус заказа.** `PRIME_ParseFlexibleDate`
  (`PRIME_00_Config.bas`) принимает "24.08", "24/08", "24-08", "24.08.2026", "24/08/2026",
  "24-08-2026" (год по умолчанию — текущий) и нормализует в "YYYY-MM-DD"; срабатывает при
  вводе "Ожидаемая дата" на листе "Заказы". `PRIME_Orders_RecomputeStatus` пересчитывает
  "Статус" (Черновик/Ожидается/Частично получено/Получено/Просрочено) при вводе количества,
  ожидаемой даты и после проведения прихода; "Отменено" — единственный статус, который
  функция никогда не перезаписывает (ручное решение пользователя). Проверено и напрямую в
  реальном LibreOffice (все 5 сценариев: ожидается/частично/получено/просрочено/отменено), и
  моделью на чистом Python (`tests/model_tests.py`).
- **Дополнительные фильтры остатка** (`PRIME_Stock_ShowByCategoryButton`/
  `ShowBySubcategoryButton`/`ShowByLocationButton` в `PRIME_09_StockSearch.bas`) — по
  категории/подкатегории/месту хранения. **Известное ограничение:** эти три функции пока не
  привязаны ни к одной кнопке шаблона (нет свободного/подходящего по смыслу элемента формы на
  листе "Остаток" под них без риска ввести пользователя в заблуждение переиспользованием чужой
  подписи) — доступны через Сервис → Макросы, полноценная кнопка на листе не добавлена.
- **Три новые проверки диагностики**: строки документов со ссылкой на несуществующий DOC_ID,
  документы без единой строки, повторные COMMITTED SOURCE_KEY в SYS_PRIME_TX.

При разработке этого пункта был найден и исправлен реальный баг, попутно подтверждающий
ценность прямой рантайм-проверки поверх статических тестов: `PRIME_ParseFlexibleDate`
изначально использовал `UBound(parts) >= 2 And IsNumeric(parts(2))` — в StarBasic `And`/`Or`
не короткозамкнуты, поэтому `parts(2)` вычислялся даже когда массив короче, и функция молча
обрывала вызывающий Sub без вылетевшего исключения (тот же класс проблем, что и другие
недокументированные особенности StarBasic в этом проекте). Обнаружено только прямым вызовом
функции в реальном LibreOffice, не статическим анализом кода.

## Не сделано в 2.0.1 (сознательно отложено, см. обоснование ниже)

Мастер-задание 2.0.1 также запрашивало полную переработку интерфейса (единый стиль верхних
панелей, новые листы "Перемещения" и "Журнал", конкретный набор кнопок по каждому листу) и
импорт заказов с превью/дедупликацией/лимитом 20 000 строк. Это НЕ сделано в данном проходе:
приоритет был отдан требованиям верхней части `priority_order` мастер-задания (отсутствие
зависаний, целостность данных, защита от двойного проведения, корректные остатки, сохранность
после закрытия) — они либо ломали работу листов целиком (см. выше), либо создавали риск тихой
порчи данных, тогда как UI-полировка и импорт — риск отсутствия функции, а не риск порчи
данных. Разработка широкого нового UI/функционала поверх ещё не до конца проверенного
фундамента увеличила бы, а не уменьшила бы вероятность недиагностированных дефектов под
давлением времени — что прямо запрещено правилом "не заявлять исправленным без свежего
доказательства". Это отдельный, самостоятельный следующий этап, а не забытая часть этого.

## Функциональные ограничения

1. **Нет UI для добавления/редактирования алиасов товара.** `DB_PRIME_ALIASES` создаётся и
   используется на чтение (`PRIME_ALIAS_AMBIGUOUS` при неоднозначном совпадении), но
   заполняется только прямым редактированием скрытого листа или отдельной миграцией — форма
   на видимом листе не реализована в этом релизе.
2. **`DB_PRIME_KITS`/`DB_PRIME_KIT_LINES` не являются фактическим источником данных
   комплектов.** Структурно созданы (`PRIME_Install_EnsureSchema`), но реальный ввод/чтение
   комплектов в первом релизе идёт через видимый плоский лист «Комплекты»
   (`PRIME_KitsColumns`, `PRIME_11_Kits.bas`). Расхождение между схемой и фактическим листом
   должно быть устранено или явно закрыто в одной из следующих версий (см. `CHANGELOG.md`).
3. **Физическая сборка/разборка комплектов (ASSEMBLY/DISASSEMBLY) не реализована.**
   `DB_PRIME_DOCUMENTS.DOC_TYPE` включает эти значения в схему, `PRIME_PostDocument`
   (`PRIME_04_Posting.bas`) не имеет для них ветки — попытка провести такой тип документа
   вернёт ошибку 1011. Текущий режим комплектов — только "набор для выдачи" (виртуального
   остатка комплекта не создаётся).
4. **Автоматическое восстановление незавершённой транзакции (crash recovery) не
   реализовано.** Если исполнение обрывается между записью `SYS_PRIME_TX` со `STATE=PREPARED`
   и `STATE=COMMITTED` (например, аварийное закрытие LibreOffice), запись остаётся видимой в
   `SYS_PRIME_TX` как диагностический след и — с 2.0.1 подтверждено фильтром
   `PRIME_IsOpIdCommitted` — гарантированно НЕ участвует в остатке (раньше это была лишь
   декларация в документации, не реализованная в коде остатка/FIFO — см. `CHANGELOG.md`).
   Но зависшая `PREPARED`-строка по-прежнему не разбирается автоматически при следующем
   открытии книги — остаётся только видимым диагностическим следом. См. `POSTING_PROTOCOL.md` §5.
5. **`SYS_PRIME_TX.HASH` не заполняется.** Колонка присутствует в схеме, но
   `PRIME_WriteTxRow` всегда пишет в неё пустую строку — проверка целостности плана документа
   по контрольной сумме не реализована.
6. **Возврат без записей `DB_PRIME_ALLOCATIONS` у исходной строки выдачи (легаси-данные)
   проводится "безлотовым" движением** (`LOT_ID=""`) на всё оставшееся количество, а не
   отклоняется — это позволяет провести документ, но ухудшает трассируемость партии для таких
   строк. См. `POSTING_PROTOCOL.md` §7 и `MIGRATION.md`.
7. **Лимит 100 строк на один документ.** `PrimeDocPlan.Lines(99)` — жёсткая граница
   (`PRIME_DOC_PLAN_MAX_LINE_INDEX`), не связана с реальным ограничением бизнес-процесса;
   документ со 101-й строкой должен быть разбит вызывающей формой на несколько операций.
8. **"Отчёт — ввод": кнопки быстрого заполнения не реализованы.** Перенесены как заглушки
   (`PRIME_UI_NotImplementedStub`) — лист доступен для просмотра/ручного ввода, автозаполнение
   из истории не работает в этом релизе.
9. **"Справочники": сохранение/обновление через кнопки не реализовано.** Аналогично — заглушка,
   прямое редактирование скрытых листов остаётся единственным способом.
10. **Несколько деструктивных кнопок очистки на панели "Инфо" намеренно заглушены**
    (`PRIME_UI_NotImplementedStub`/аналог), чтобы не переносить в PRIME небезопасные операции
    массовой очистки данных из 1.4.1 без отдельного осознанного проектирования.

## Ограничения тестового покрытия (см. также TEST_MATRIX.md)

11. **`tests/functional_smoke.py` не подтверждает работоспособность интерактивного потока
    "создать заказ → провести → проверить остаток" в реальном исполнении.** В этом
    headless-окружении LibreOffice многошаговые cепаратные external `invoke()`-цепочки (см.
    `test_smoke_single_invoke.py` в истории отладки) ведут себя ненадёжно: после
    `PRIME_Orders_ConductRow` наблюдаемое состояние листа не отражает ожидаемого эффекта, при
    этом ни `On Error`, ни файловый лог изнутри Basic не дают воспроизводимого диагностического
    сигнала — похоже на особенность конкретной комбинации LibreOffice/среды с внешним
    UNO-`invoke()`, а не на подтверждённый дефект бизнес-логики. Это **честно
    задокументированное ограничение среды тестирования, а не подтверждение того, что
    проведение работает или не работает в реальном интерактивном использовании**. Логика
    алгоритмов (FIFO, идемпотентность, лимит возврата, сохранение остатка при перемещении)
    отдельно проверена как чистая Python-модель в `tests/model_tests.py`, что НЕ эквивалентно
    рантайм-проверке самого Basic-кода.
12. **Структурная валидация (`tests/static_checks.py`) не проверяет бизнес-логику**, только то,
    что собранный `.ods` структурно корректен: все модули встроены, системные листы существуют
    и скрыты, кнопки/события не ссылаются на удалённые Sub, легаси WMSDB-код отсутствует.
13. **Ручная интерактивная проверка в реальном LibreOffice (не headless-автоматизация)** после
    сборки не проводилась в рамках данной разработческой сессии из-за ограничений среды
    (отсутствие интерактивного дисплея) — рекомендуется перед первым реальным использованием.

## Известные архитектурные компромиссы (не дефекты, осознанные решения)

14. Партии одного товара сортируются простой сортировкой вставками (`PRIME_FifoLotsForProduct`)
    — рассчитано на десятки/сотни партий на товар, не тысячи; при существенно большем объёме
    потребуется индексация.
15. `PRIME_AuditLog` — единственное место в кодовой базе с намеренным `On Error Resume Next`
    (сбой логирования не должен ронять проведение); это осознанное исключение из общего
    правила «без подавления ошибок», а не пропущенная проверка.
16. **Реестр `PRIME_FormSchemaHeaderRow` (header_schema_registry, 2.0.1) хранит номер строки
    заголовка как константу по имени листа**, а не определяет его динамически по содержимому.
    Если пользователь вручную вставит/удалит строку в декоративной "шапке" листа с header row
    4/5 (Приход/Расход-Цех/Офис, Возвраты, Инвентаризация, Остаток, Остаток-Заказы,
    База-Поиск), реальный заголовок физически сместится, а код продолжит читать по старому
    номеру строки — это может увидеться как "пропавшие"/смещённые колонки. Не защищено
    проверкой на этот случай; обычная работа (ввод данных, использование кнопок) эту шапку
    не трогает, риск только при ручном редактировании структуры листа.
